/**
 * @deprecated These components have been moved to /components/ui/file/file-preview.
 * Please update your imports to use the new path.
 */

import { FileViewer, FileIcon, ImageEditorPreview } from '../file/file-preview'

/**
 * @deprecated Use FileViewer from '@/components/ui/file/file-preview' instead.
 */
export const FilePreview = FileViewer

export { FileIcon, ImageEditorPreview }
