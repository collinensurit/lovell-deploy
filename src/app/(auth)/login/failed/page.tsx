export const dynamic = 'force-dynamic';
export const revalidate = 0;
import { FailedLogin } from '@/components/auth/failed-login'

export default function FailedLoginPage() {
  return <FailedLogin />
}
